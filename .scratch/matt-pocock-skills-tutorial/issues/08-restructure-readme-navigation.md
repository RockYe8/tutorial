# 08 — 重构 README 为导航页

**What to build:** Turn the tutorial README into a clean navigation and routing page that sets reader expectations, removes context-free prompt templates, and makes the scenario table easier to scan.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] README explicitly says the tutorial is not Matt Pocock official documentation or an official translation.
- [ ] README no longer contains a standalone "可复制调用模板" section.
- [ ] README directs readers to contextual examples inside tutorial sections instead of encouraging context-free copy/paste.
- [ ] The scenario routing action column uses "建议下一步" or equivalent wording, not "优先使用的 Skill".
- [ ] The long scenario routing table is split into four groups: 新手入门, 主链路, Shaping 与回流, 维护与异常.
- [ ] `/research`, `/prototype`, and `/wayfinder`回流 are not collapsed into one ambiguous row.
- [ ] `/wayfinder` is not described as normally returning to `/grill-with-docs`.
- [ ] README link text includes clear article and section names so readers can recover when local preview only opens the target file.
- [ ] README remains a navigation page, not a full conceptual article.

